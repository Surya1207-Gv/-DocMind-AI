# Development & CI dependencies (not installed in the production image).
-r requirements.txt

pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.28.1                    # required by fastapi.testclient.TestClient (and by google-genai)
