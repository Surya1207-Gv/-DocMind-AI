# ---------------------------------------------------------------------------
# DocMind AI — runtime dependencies
# ---------------------------------------------------------------------------
# Pinned to the exact versions the application is tested against, so a clean
# deploy reproduces local behaviour. Test-only packages live in
# requirements-dev.txt and are deliberately kept out of the production image.

# --- Web framework ---
# 0.115.x pulls starlette>=0.40, whose TestClient works with httpx 0.28 (the
# version google-genai requires). Pinning fastapi 0.110 broke the whole API test
# suite with `TypeError: Client.__init__() got an unexpected keyword 'app'`.
fastapi==0.115.14
uvicorn[standard]==0.28.0
python-multipart==0.0.9          # multipart/form-data parsing for PDF uploads
pydantic==2.13.4

# --- Auth ---
pyjwt==2.8.0
bcrypt==4.1.2

# --- RAG: parsing, chunking, vectors ---
pypdf==4.1.0
langchain-text-splitters==1.1.2  # RecursiveCharacterTextSplitter
langchain-core==1.5.3            # Document / Embeddings / BaseChatModel interfaces
langchain-community==0.4.2       # FAISS vectorstore wrapper
faiss-cpu==1.8.0
numpy==1.26.4                    # faiss-cpu 1.8.0 is built against the numpy 1.x ABI

# --- Agent orchestration ---
langgraph==1.2.10                # StateGraph multi-hop agent (plan/retrieve/synth/verify)

# --- LLM providers ---
langchain-google-genai==4.3.3    # optional Gemini fallback provider
requests==2.34.2                 # OpenRouter chat + embeddings HTTP calls
tenacity==8.5.0                  # retry/backoff around transient LLM failures

# --- Config ---
python-dotenv==1.0.1
